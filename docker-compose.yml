services:
  birthday-site:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8080:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true